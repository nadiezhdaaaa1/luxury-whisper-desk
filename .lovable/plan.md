Give the "See how it works" button a solid background matching the hero.

In `src/styles.css`, change the `btn-ghost` `background-color` from `transparent` to `var(--color-background)` so the button reads as a solid pill in the hero's ivory tone (dots behind won't show through).