Shift the Rolex image further left so more of it bleeds off the left edge.

In `src/components/landing/Hero.tsx` (Target reached card image), change `-ml-10` to `-ml-24` to push the image further past the card's left border.