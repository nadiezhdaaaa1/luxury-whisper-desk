Change the heading container in `src/components/landing/Comparison.tsx` (line 39) from `max-w-[480px]` to `max-w-[672px]` so the "Not a spreadsheet. Not a marketplace." block matches the standard section width used elsewhere.